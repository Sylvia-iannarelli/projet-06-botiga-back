{% extends 'layout.html.twig' %}
{% block title %}Paiement{% endblock %}

{% block subBody %}
	{{ include('_flash_messages.html.twig') }}
	<h1 class="mt-5">Modifier le paiement</h1>

	{{ include('payment/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}

	<a href="{{ path('app_payment_index') }}" class="btn btn-primary me-2">Retour à la liste</a>

	{{ include('payment/_delete_form.html.twig') }}

{% endblock %}
