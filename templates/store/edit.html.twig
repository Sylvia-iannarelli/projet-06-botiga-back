{% extends 'layout.html.twig' %}
{% block title %}Boutique
{% endblock %}

{% block subBody %}
	<h1 class="mt-5">Modifier la boutique
	</h1>

	{{ include('store/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}

	{% if is_granted('ROLE_ADMIN') %}
		<a href="{{ path('app_store_index') }}" class="btn btn-success me-2">Retour à la liste</a>
	{% endif %}

	{% if is_granted('ROLE_ADMIN') %}
		{{ include('store/_delete_form.html.twig') }}
	{% endif %}

{% endblock %}
